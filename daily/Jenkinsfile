pipeline{
    agent master
    stages{
        stage('Daily Build'){
            steps{
                script{
                    echo "Executing Daily Build on ${env.JENKINS_URL}"
                }
            }
        }
    }
}
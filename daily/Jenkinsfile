pipeline{
    agent any
    stages{
        stage('Daily Build'){
            steps{
                script{
                    echo "Executing Daily Build on ${env.JENKINS_URL}"
                }
            }
        }
    }
}

pipeline{
    agent any
    
    /*triggers {
        cron('* * * * *')
    }*/
    options {
        buildDiscarder(logRotator(numToKeepStr: '7'))
    }
    
    stages{
        stage('Daily Build'){
            steps{
                script{
                    echo "Executing Daily Build on ${env.JENKINS_URL}"
                }
            }
        }
    }
}
